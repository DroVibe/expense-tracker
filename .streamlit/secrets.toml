# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# SUPABASE SECRETS — Co-Parent Expense Tracker
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 1. Create a free Supabase project at https://supabase.com
# 2. In the SQL Editor, run:
#      CREATE TABLE expenses (
#        id          SERIAL PRIMARY KEY,
#        date        DATE,
#        description TEXT,
#        category    TEXT,
#        amount      NUMERIC,
#        paid_by     TEXT,
#        split_pct   NUMERIC DEFAULT 50,
#        status      TEXT DEFAULT 'active',
#        notes       TEXT,
#        receipt_url TEXT,
#        created_at  TIMESTAMP DEFAULT NOW()
#      );
# 3. In Storage, create a public bucket named "receipts"
# 4. Go to Settings → API and copy:
#      Project URL  → paste below after SUPABASE_URL
#      anon public  → paste below after SUPABASE_KEY
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

SUPABASE_URL = "your_url_here"
SUPABASE_KEY  = "your_anon_key_here"
